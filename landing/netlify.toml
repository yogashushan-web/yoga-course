[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Run follow-up emails every day at 6:00 UTC (9:00 Israel time)
[functions."send-followups"]
  schedule = "0 6 * * *"
